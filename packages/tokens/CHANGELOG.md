# Changelog

All notable changes to `@massds/mds-tokens` should be documented in this file.

This package uses changelog fragments in `changelog.d/`. Before a release:
1. Add fragment files under `packages/tokens/changelog.d/`
2. Run `npm run changelog:release -- <version> <date>` from `packages/tokens`
3. Remove released fragment files after the release is cut