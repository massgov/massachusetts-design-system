<span class="mds-icon mds-icon--weight-{{ weightKey|e }}{% if className %} {{ className|e }}{% endif %}"{% if decorative %} aria-hidden="true"{% elseif ariaLabel %} role="img" aria-label="{{ ariaLabel|e }}"{% endif %}>{{ svg|raw }}</span>
