import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Overview/Introduction" />

# Massachusetts Design System

This Storybook documents the Massachusetts Design System packages and previews
Twig-authored static components as they are added to `packages/components`.

The component stories consume package exports instead of local source paths so
the documentation site behaves like a downstream application.
