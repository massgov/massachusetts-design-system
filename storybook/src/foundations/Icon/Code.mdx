import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
import { iconDefaults } from '../../../../packages/components/src/icon/icon.data.js';
import iconCss from '@massds/mds-components/icon.css?raw';
import { renderIcon } from '../../utils/component-renderers.js';
import { StoryCodeTabs } from '../../utils/CodeTabs/index.jsx';
import * as IconStories from './icon.stories.js';

export const getIconCodeTabs = (args) => [
  {
    label: 'HTML',
    language: 'html',
    code: renderIcon({
      ...iconDefaults,
      ...args
    })
  },
  {
    label: 'CSS',
    language: 'css',
    code: iconCss
  }
];

<Meta of={IconStories} />

# Icon

<Canvas
  of={IconStories.Playground}
  sourceState="none"
/>

## API

<Controls of={IconStories.Playground} />

## Code

<StoryCodeTabs of={IconStories.Playground} getTabs={getIconCodeTabs} />